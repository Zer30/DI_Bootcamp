// 1 The keyup event fires after the key is released, making it suitable for modifying the input value after a keypress.
// const inputElement = document.getElementById('letterInput');
// inputElement.addEventListener('keyup', function(event) {
//     event.target.value = event.target.value.replace(/[^a-zA-Z]/g, '');
// });

// // 2 The keydown event fires when a key is pressed, making it suitable for detecting the key being pressed before the input value is modified.
// const inputElement = document.getElementById('letterInput');
// inputElement.addEventListener('keydown', function(event) {
//     event.target.value = event.target.value.replace(/[^a-zA-Z]/g, '');
// });

